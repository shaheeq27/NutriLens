# NutriLens backend — runtime + dev dependencies
# Path in repo: backend/requirements.txt
#
# Kept in sync with backend/pyproject.toml. If you add/remove a dependency
# there, mirror the change here too — pip installs from this file don't
# read pyproject.toml, so the two can silently drift.

# --- Web framework ---
fastapi>=0.110
uvicorn[standard]>=0.29

# --- Validation / settings ---
pydantic>=2.5
pydantic-settings>=2.4

# --- File uploads (FastAPI UploadFile / form parsing needs this explicitly) ---
python-multipart>=0.0.9

# --- Image handling (decode, dimension/megapixel checks, EXIF stripping) ---
Pillow>=10.0

# --- HTTP client (USDA FoodData Central + outbound vendor calls) ---
httpx>=0.27

# --- Vision / OCR provider SDKs ---
openai>=1.0
google-cloud-vision>=3.0

# --- Dev / test dependencies ---
pytest>=8.0
pytest-asyncio>=0.24